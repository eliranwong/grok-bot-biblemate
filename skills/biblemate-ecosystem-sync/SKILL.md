---
name: BibleMate Ecosystem Sync
description: "Slash alias for GBM Ecosystem Sync. Use when the user runs /biblemate-ecosystem-sync."
ecosystem: biblemate
port: grok-bot-native-alias
target: gbm-ecosystem-sync
---
# BibleMate Ecosystem Sync

Native Grok Bot slash alias for **GBM Ecosystem Sync**.

## Steps
1. Open and follow `/home/box/agent-data/biblemate-native-skills/gbm-ecosystem-sync/SKILL.md` exactly.
2. Pass through all arguments after `/biblemate-ecosystem-sync`.
3. Scope is **only** `/workspace/grok-bot-biblemate` — never a generic `/sync`.
4. `SendToUser` the sync result when finished.
