---
name: Locations
description: "Retrieve coordinates, maps, etymological details, and historical/geographical/theological significance of Bible locations. Use when the user runs /locations or requests this BibleMate workflow."
ecosystem: biblemate
port: grok-bot-native
source: .grok/skills/locations
---
# Locations

## Grok Bot runtime notes (native port)

This skill was ported from the BibleMate Grok Build `.grok` playbook for **native Grok Bot** execution.

### Tool mapping
| Playbook concept | Use on Grok Bot |
|------------------|-----------------|
| Web research | `WebSearch`, `WebFetch` |
| Read files | `Read` |
| Shell / scripts | `Shell` (`python3` on helpers beside this playbook under `/home/box/agent-data/biblemate-native-skills/`) |
| Parallel workers | `Task` with `subagent_type: executor` (or MessageSubagent to steer) |
| Save reports | Write the **full, uncompromised** report under `/workspace/grok-bot-biblemate/` in the folders the playbook names (`biblemate/`, `devotions/`, `sermons/`, `studies/`, …) |
| User-visible result | Always `SendToUser` a short digest **and** a chat copy of the report (paste full text or attach the saved `.md`) + concrete path when finished |

### Rules
- Do **not** shell out to `grok`/`claude`/`agy` for this skill — execute the methodology yourself with the tools above.
- **Never invent Scripture text.** Fetch verses via the `bible` skill / `bible_retriever.py` (local SQLite under `/workspace/biblemate/data/bibles` and `/workspace/biblemate/data_custom/bibles`; also mirrored at `/home/box/biblemate/...`).
- Default translations when unspecified: **CSB** (English), **CUV** (Chinese). Prefer Cantonese (粵語) for devotionals/check-ins when the user's language preference suggests it.
- Adopt a warm pastoral tone (Compassionate Pastor / related personas from BibleMate agents.md) unless the skill names a different persona.
- Prefer parallel tool calls where the playbook says to run work together.
- Working directory for artifacts: `/workspace/grok-bot-biblemate` (report folders sync to git — do not treat as disposable cache).
- On every finished run: full report on disk **and** chat copy via `SendToUser` (attach `.md` for long reports).
- When the playbook mentions sibling skills, read the full playbook under `/home/box/agent-data/biblemate-native-skills/<id>/SKILL.md` (catalog loaders under `/home/box/agent-data/workflows/` point there after install).
- Helper scripts live beside the playbook after install, e.g. `python3 /home/box/agent-data/biblemate-native-skills/bible/bible_retriever.py "<query>"`.

---
# Locations Retrieval and Significance Skill

## Overview
This skill allows the agent to retrieve geographical coordinates, maps, and etymological/historical descriptions of locations mentioned in the Bible, and then conduct a comprehensive study of their historical, geographical, and theological significance.

## Guidelines & Objectives
When executing this skill:
- Always run the python retriever script located at `/home/box/agent-data/biblemate-native-skills/locations/locations_query.py` to retrieve location data.
- Execute the script using: `python3 /home/box/agent-data/biblemate-native-skills/locations/locations_query.py "<query>"` where `<query>` is the name of the location.
- The script automatically handles fuzzy spelling matching (Levenshtein distance) for locations with minor spelling variations.
- Present the exact output of the script to the user. The script filters out embedded HTML map elements but preserves the Live Google Map link, coordinate details, dictionary descriptions, and occurrences in scripture.
- Keep explanations biblically accurate, structurally clear, and cite specific book, chapter, and verse references for all statements.
- All study outputs generated by the `/locations` command must be saved to the `biblemate/` folder with a timestamp prefix under the Universal Study Output Saving Rule.

## Detailed Exegesis & Significance Analysis
After presenting the retrieved details from the script, expand upon the location's significance by addressing:
1. **Geography & Topography**: Location, physical characteristics, and strategic importance in the ancient world.
2. **Historical Timeline**: Major events associated with this place throughout biblical and secular history.
3. **Theological Significance**: Covenants, prophecies, or symbols connected to the location.
4. **Redemptive Fit**: How this place fits into the geography of salvation history (e.g., wilderness, Jerusalem, Babylon).
- If a singular name is used to denote various different locations in the Bible, kindly provide separate descriptions and analysis for each one.

## Slash command guidance

Folded from the Grok Build command `/locations` — treat “user arguments after the slash command” as the input the user provided after the slash.

Adopt the **OT Bible Scholar** (for Old Testament locations), **NT Bible Scholar** (for New Testament locations), or **Biblical Linguistic Analyst** persona from `/workspace/biblemate-agentic-workspace/.grok/agents.md`.

Use the **locations** skill to retrieve geographical coordinates, maps, etymology, and scriptural occurrences for the given location. Then, provide a comprehensive analysis of the location's historical timeline, geographical importance, and redemptive-theological significance in the Bible.

If the location name is used to denote various different places in the Bible, kindly provide separate descriptions and analysis for each one.

# Location name

user arguments after the slash command

